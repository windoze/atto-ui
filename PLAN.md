# 执行计划：Turbo Vision UI 对齐

本计划对应 [`UI_GAPS.md`](UI_GAPS.md) 中的 14 项差异，目标是让 atto-ui 的渲染与交互更贴近经典 Turbo Vision (TV)。

任务清单见 [`TODO.md`](TODO.md)。

## 原则

- 默认主题与现有行为尽量保持兼容；TV 风格通过**新增**主题提供，不强制改默认。
- 每个可见改动都补一条 PTY 快照集成测试（`tests/`，参考 `pty_desktop.rs`）。
- 优先做对「像不像 TV」提升最大的窗口装饰与按钮。

## 阶段划分

### 阶段 1 — 窗口装饰与按钮（高优先级）
对应 GAP #1 #2 #3 #4。这是视觉冲击最大的一组。

- **#1 标题居中 + 空格包裹**：`src/wm/manager/chrome.rs`，顶边标题居中、前后留 1 空格。
- **#2 关闭/缩放钮归位**：关闭钮 `[■]` 移到左上角，缩放钮 `[↑]`/`[↕]` 右上角，统一 `[ ]` 包裹；glyph 在 `src/theme/mod.rs`。需同步更新命中测试（拖动/点击关闭区域的坐标）。
- **#3 按钮重绘**：`src/widgets/button.rs`，单行色块 + 右/下阴影 + 默认按钮强调，去掉三行边框。注意按钮高度从 3 变 1，检查依赖按钮尺寸的布局与测试。
- **#4 桌面背景纹理**：`src/app/desktop.rs:580`，桌面 `Fill` 用 `░` 纹理。

依赖：#2 改动会动到窗口 chrome 的命中区域，需与 `src/wm/manager/` 的鼠标处理一起回归。

### 阶段 2 — 菜单条（中优先级）
对应 GAP #5 #6 #7。

- **#5 菜单条整体化**：`src/app/menu/draw.rs` `MenuBar::draw`，先用 `menu_bar` 样式填满整行，再绘制各项，使项间空隙与右侧空白同色。
- **#6 点击只高亮、无下沉阴影**：确保顶层菜单项激活仅切 `menu_bar_active`，不调用 `draw_shadow`。
- **#7 热键字母配色**：`mnemonic_style` / 主题键 `menu-mnemonic` 用 accent 色（经典红）。

### 阶段 3 — 状态栏与滚动条（中优先级）
对应 GAP #8 #9。

- **#9 状态栏 item 可点击**：`src/app/desktop.rs:602` 默认状态栏改用 `StatusSegment` 渲染并接命令回调（基础设施 `StatusSegment::on_click` 已就绪）。
- **#8 滚动条箭头/轨道**：composable/scroll 渲染，保证 `▲`/`▼` 端帽与 `░` 轨道在矮内容区也渲染。

### 阶段 4 — TV 配色主题（中优先级）
对应 GAP #10。

- 新增 `Theme::turbo()`（蓝桌面 / 灰青菜单状态栏 / 灰底对话框 / 绿色选中高亮），并可通过主题文件加载。默认主题不变。

### 阶段 5 — 细节字形（低优先级）
对应 GAP #11 #12 #13 #14。

- 复选框 `[x]`→`[X]`；单选 `(*)`→`(•)`；可选 `≡` 系统菜单图标；顶层菜单项间距微调。

## 验证

- 每阶段结束运行 `cargo test`（含 PTY 集成测试）与 `cargo clippy`。
- 关键视觉项用 `snapshot_app` 抓取屏幕快照人工比对参考截图。
