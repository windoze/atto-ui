# UI 对齐差异清单 (atto-ui vs Turbo Vision)

本文档记录 atto-ui 当前渲染与经典 Turbo Vision (TV) 的明显视觉差异，作为一轮 UI 对齐的工作清单。

参考截图：现代 tvision (tvedit) 与经典 Turbo C++ IDE。

对比基于真实 PTY 渲染抓取 (`snapshot_app`, 默认 `Theme::dark()`) 与代码走读。

---

## 🔴 高优先级（结构性，一眼就不像）

### 1. 窗口标题未居中、无空格包裹
- **现状**：`╔Widgets════════════…` —— 标题紧贴左上角，无 padding。
- **TV**：标题居中，两侧空格隔开，如 `══════ /home/.../hello.cpp ══════`。
- **目标**：标题在顶边居中，前后各留 1 空格。
- **涉及**：`src/wm/manager/chrome.rs`

### 2. 关闭按钮位置 / 样式不同
- **现状**：左上角无关闭钮；最小化/最大化/关闭挤在右上角且无方括号 —— `…−═□═×╗`。
- **TV**：关闭钮 `[■]` 在**左上角**，缩放钮 `[↑]`/`[↕]` 在**右上角**，均带 `[ ]` 包裹。
- **目标**：关闭钮移到左上角用 `[■]`；右上角缩放钮 `[↑]`（或 `[↕]`）；统一加方括号。
- **涉及**：`src/wm/manager/chrome.rs`，glyph 在 `src/theme/mod.rs`

### 3. 按钮被画成三行带框小盒子
- **现状**：`┌──────┐ / │ OK │ / └──────┘`（整框边框，3 行高）。
- **TV**：按钮是单行色块 `  OK  `，右/下带阴影，默认按钮高亮（绿/青底），无边框。这是 TV 对话框最标志性的元素，目前差异最大。
- **目标**：按钮渲染为单行 + 阴影 + 默认按钮强调，去掉边框。
- **涉及**：`src/widgets/button.rs`

### 4. 桌面无背景填充纹理
- **现状**：桌面纯黑/空白。
- **TV**：桌面铺满 `░` 点阵（经典蓝底点纹）。
- **目标**：桌面背景用 `░` 纹理填充。
- **涉及**：`src/app/desktop.rs:580` (`Fill { style: theme.desktop }`)

---

## 🟡 中优先级

### 5. 菜单条应显示为一个整体（背景统一）
- **现状**：`desktop.rs:580` 先用 `theme.desktop` 填满整屏，`menu.draw` 只按「标题宽 + 2」逐项填 `menu_bar` 色；菜单项之间的空隙与右侧空白露出的是**桌面背景**，菜单条不是一整条。
- **目标**：整条菜单栏（含项间空隙与右侧空白）默认背景与 `menu_bar` 一致，呈现为连续色带。
- **涉及**：`src/app/menu/draw.rs` (`MenuBar::draw`)，先以 `menu_bar` 样式填满整行再绘制各项。

### 6. 菜单项点击/激活只高亮、不要下沉阴影
- **现状**：菜单条上的菜单项被点击时出现下沉/阴影效果。
- **目标**：菜单条上的菜单项被点击或激活时，仅切换为高亮样式 (`menu_bar_active`)，不画任何阴影 / 下沉效果。
- **涉及**：`src/app/menu/draw.rs`（确保顶层菜单项不调用 `draw_shadow`，仅切换样式）。

### 7. 菜单热键字母无配色
- **现状**：`File   Help`，热键只下划线、无对比色。
- **TV**：每个菜单项首字母（加速键）用醒目对比色（截图为红色），如 **F**ile **E**dit **S**earch。
- **目标**：mnemonic 字母用 accent 色（经典 TV 为红色）。
- **涉及**：`src/app/menu/draw.rs` (`mnemonic_style`)，主题键 `menu-mnemonic`

### 8. 滚动条缺少箭头端帽 / 轨道
- **现状**：List/Table 右侧只见 `█` 滑块，未见 `▲`/`▼` 箭头与 `░` 轨道（矮列表尤甚）。
- **TV**：滚动条顶端 `▲`、底端 `▼`，中间 `░` 轨道 + 滑块。
- **目标**：确认箭头端帽与轨道始终渲染（即使内容区很矮）。
- **涉及**：composable/scroll 渲染，glyph 在 `src/theme/mod.rs`

### 9. 状态栏 item 应可点击（hotspot）
- **现状**：基础设施已具备 —— `StatusSegment::on_click` + `StatusBar::handle_mouse` 命中测试（见 `segment_click_hit_test_invokes_callback`）。但默认状态栏走 `desktop.rs:602` 的 `set_left` 纯字符串路径，渲染为纯文本，未接 click。
- **TV**：状态栏每个 item（`F2 Save`、`F3 Open`…）都是可点击 hotspot，点击即触发对应命令（等价于按该快捷键）。截图上看不出，但是 TV 的标准交互。
- **目标**：默认状态栏改用 segment 渲染，并把每个 key 标签接上对应命令回调，使其可点击。
- **涉及**：`src/app/desktop.rs:602` 起的默认状态栏构建、`src/app/status.rs`

### 10. 新增一套 TV 配色主题（不改默认）
- **现状**：现代深色（黑底、灰字、LightBlue 点缀）。仅有 dark / light 两套。
- **TV**：蓝桌面 / 灰青菜单栏与状态栏 / 灰底对话框 / 绿色高亮选中项。
- **目标**：**新增**一套独立的 "turbo"（classic TV）配色主题，精确还原经典调色板；**默认主题保持不变**，不要求默认与 TV 一致，只是多提供这一套可切换的主题。
- **涉及**：`src/theme/mod.rs`（新增 `Theme::turbo()` 之类的构造），可同时支持通过主题文件加载。

---

## 🟢 低优先级（细节字形）

### 11. 复选框字形
- 现状 `[x]` → TV `[X]`（大写）。`src/theme/mod.rs`

### 12. 单选按钮字形
- 现状 `(*) Normal` → TV 圆点 `(•)` / `( )`。`src/widgets/radio.rs`

### 13. 菜单栏左侧系统菜单图标
- TV 经典 IDE 有 `≡` 汉堡图标，我们没有（可选）。

### 14. 顶层菜单项间距
- 现状 3 空格分隔，TV 间距更紧凑统一（视配色而定，次要）。

---

## 已对齐、无需改动

- 活动窗口双线边框 `╔═╗` vs 非活动单线 `┌─┐`
- 下拉菜单单线框 + 阴影 + 子菜单 `▶` 箭头 + 右对齐快捷键
- 状态栏 key 用对比色高亮

---

## 建议实施顺序

对「像不像 TV」提升最大的优先：**1（标题居中）→ 2（关闭钮归位）→ 3（按钮重绘）→ 5/6（菜单条整体化、去阴影）**。
